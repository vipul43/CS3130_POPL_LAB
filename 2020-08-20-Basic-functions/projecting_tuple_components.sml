(*  Question-2
    fst(it is a function) type => 'a * 'b -> 'a
    snd(it is a function) type => 'a * 'b -> 'b
    x	      		  type => 'a
    y			  type => 'b
*)
fun fst (x, y) = x;
fun snd (x, y) = y;
